[package]
name = "stoicn"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rayon = "1.12.0"
rustc-hash = "2.1.2"

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
